import React from 'react';
import {StyleSheet, Text, View} from 'react-native';

const ChatList = () => {
  return (
    <View>
      <Text>This is the chat list page</Text>
    </View>
  );
};

export default ChatList;

const styles = StyleSheet.create({});
